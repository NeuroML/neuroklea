name: Ruff
on:
  pull_request:
    branches:
      - main
      - development
      - experimental
      - 'test*'
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: chartboost/ruff-action@v1
        with:
          changed-files: 'true'
