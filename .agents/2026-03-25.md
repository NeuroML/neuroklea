# Session: 2026-03-25

**Authoring agent:** opencode/big-pickle

## Objective
Restructured AGENTS.md for multi-package repo (top-level + per-package).

## Completed
- Created per-package AGENTS.md files for all 5 packages
- Updated top-level AGENTS.md with session logging and git conventions
- Ran ruff/pre-commit, committed in two chunks, pushed

## Completed
- Created per-package AGENTS.md files for all 5 packages
- Updated top-level AGENTS.md with session logging and git conventions
- Ran ruff/pre-commit, committed in two chunks, pushed

## Completed (continued)
- Added session support to rag_pkg CLI (uuid import, session_id generation, json payload)
- Fixed HTTP 422 errors by changing `data=` → `json=` in rag_pkg UI files (cli.py, streamlit_ui.py)
- Added session support to neuroml_ai package (api/main.py, api/chat.py, ui/cli.py, ui/streamlit_ui.py)
- Updated neuroml_ai/api/chat.py to pass thread_id to run_graph_invoke
- Reviewed rag_pkg fallback/non_domain_chat logic - confirmed fix for incorrect attribute name

## Errors and Lessons
- Pydantic field access errors not caught by ruff/mypy at static analysis time - need pyright for strict mode

## Open Questions
- Fallback behavior: user to test manually
- Static analysis for Pydantic: deferred adding pyright for now
