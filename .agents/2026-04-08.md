# Session: 2026-04-08

**Authoring agent:** opencode

## Objective
Design exploration phase for CodeAI agent graph with parallel tool call support.

## Completed
- Renamed `tool_picker.py` → `tools_picker.py` and `tool_caller.py` → `tools_caller.py` (class names: `ToolPicker` → `ToolsPicker`, `ToolCaller` → `ToolsCaller`)
- Updated imports and node references in `code_ai.py`
- Renamed and updated `ToolPicker_system.md` → `ToolsPicker_system.md` (content updated to reflect multiple tools)
- Created `tools_router.py` node inheriting from `AbstractRouterNode` (empty execute method - user will fill)
- Discussed parallelization strategy: try-catch approach - attempt parallel by default, fall back on failure
- Discussed state design: single `task_plan` with exploration steps included (not separate `exploration_plan`)
- Schema update: `step_outputs: Dict[int, list[CallToolResult]]` for storing tool outputs linked by step index

## Key Decisions
- Explicit ExplorePlanner needed because smaller models (e.g., qwen3:0.6b) may not implicitly explore
- Tools observer only checks tool outputs (did all tools run?), doesn't need to know exploration vs execution
- Try-catch for parallel: schema validation catches malformed calls early, runtime retry for actual failures (>3 attempts)
- Tools router gets outputs from tools_caller and routes to next step (leave execute empty for now)

## Open Questions
- Tools router logic to implement (user needs to write)
- How tools_observer ties into flow
- Retry logic specifics (per-call vs per-step)

## Notes
- Flow: ExplorePlanner → tools_picker → tools_caller → tools_router → step_router → Planner
- tools_router is the "tools observer" - routes based on tool outputs
- step_router handles higher-level step routing

## References
- Files: `code_ai_pkg/neuroml_code_ai/nodes/tools_picker.py`, `tools_caller.py`, `tools_router.py`
- Prompts: `code_ai_pkg/neuroml_code_ai/nodes/prompts/ToolsPicker_system.md`
