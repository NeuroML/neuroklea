# Session: 2026-06-23

**Authoring agent:** opencode

## Objective
Rewrote AGENTS.md, refactored stores.py multi-backend support.

## Completed
- Rewrote AGENTS.md with only verifiable repo facts.
- Fixed `stores.py`: lazy-load caching, ImportError hints per backend, Qdrant -> QdrantVectorStore.
- Added TODO for Qdrant/PGVector tests.

## Errors and Lessons
- CLAUDE.md is a symlink to AGENTS.md -- writing to either destroys both. Noted at top of AGENTS.md.

## Notes
- Qdrant `url=` kwarg was silently broken (wrong API).
- `QdrantVectorStore` uses `embedding=` not `embeddings=`.
