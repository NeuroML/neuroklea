# Session: 2026-06-29

**Authoring agent:** opencode

## Objective
Add documentation to the Klea project -- landing page, install guide, CLI reference, API reference -- deployable to GitHub Pages at neuroklea.org.

## Completed
- Researched documentation tool options (Pelican, MkDocs, Sphinx, Hugo, Docusaurus, etc.) and the collapsing MkDocs ecosystem -- settled on Sphinx + Furo
- Created full docs scaffold: conf.py, Furo theme, sphinxcontrib-typer for CLI autogen, autodoc for API ref
- Landing page, install guide, contributing guide (with AI use policy)
- CLI reference auto-generated from Typer app objects via sphinxcontrib-typer (5 entry points)
- API reference for klea_utils only (app packages dropped -- their Python API isn't meant for external use)
- GitHub Actions workflow with path filtering and gh-pages deploy
- Contributing guide AI use policy section
- Code of conduct page linking to NeuroML CoC
- paths-ignore on general CI to skip docs-only changes

## Errors and Lessons
- mcp PyPI package 1.28+ crashes under pydantic 2.13 with `extra="allow"` -- autodoc imports fail. Workaround: autodoc_mock_imports.
- Mocking fails on Python `|` union type annotations -- dropped `nodes.abstract`/`base` from API docs.
- sphinx-click broken with Python 3.13; sphinxcontrib-typer worked but needs typer < 0.26.8 pinned.
- First GitHub Pages deploy with GITHUB_TOKEN requires manual gh-pages branch selection in Settings after first (expected 403) failure.

## Notes
- All packages used GPL-3.0-or-later with MIT files -- user fixed the mismatch.
- All Python files already had "Copyright 2026" headers -- no update needed.
- 4 unicode em-dashes fixed per ASCII-only convention.
- Tutorial and domain config still pending.
