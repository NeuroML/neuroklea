# Session: 2026-06-18

**Authoring agent:** opencode

## Objective
Bundled default tools for Klea Code via stdio MCP server, matching rag_pkg's config pattern.

## Completed
- `code_pkg/klea_code/tools/bundled.py` — FastMCP stdio server with `list_files` tool
- `KleaCode._configure_resources()` — merges bundled + external MCP servers
- `AppEnv` for env vars, `AppConfig(mcp_servers)` for JSON (simple, no domains)
- `_build_tools_description()` — `num_servers` check for prefix/no-prefix modes
- `ToolsCaller` — `Client | None` guard, `async with` per call
- `Planner`/`ToolsPicker` accept `dict[str, str]` for `tools_description`

## Reverted
- Domain-aware config/filtering — code_pkg not domain-based. LLM sees all tools flat like OpenCode.

## Notes
- 21/22 tests pass (1 pre-existing test_stores failure)
- `code_pkg` previously uninstantiable — now fixed
